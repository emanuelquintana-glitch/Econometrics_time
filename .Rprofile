# .Rprofile para Econometrics Time Series
options(
  repos = c(CRAN = "https://cloud.r-project.org"),
  scipen = 999,
  digits = 4,
  width = 80
)

# Mensaje de bienvenida
message("📊 Econometrics Time Series Project")
message("Working directory: ", getwd())

# Cargar funciones personalizadas al iniciar
if (file.exists("R/functions/econometrics_utils.R")) {
  source("R/functions/econometrics_utils.R")
}
